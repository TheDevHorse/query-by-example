type Athlete {
    id: ID!
    age: Int!
    name: String!
    address: Address
}

type Address {
    city: String!
    street: String!
    zipCode: String!
}

# (Queries)
type Query {
    athlete(id: ID!): Athlete
    athletes: [Athlete]
}